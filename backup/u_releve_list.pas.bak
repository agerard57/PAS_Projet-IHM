unit u_releve_list;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, Forms, Controls, Graphics, Dialogs, u_liste;

type
  Tf_releve_list = class(TF_liste)
 procedure Init (affi : boolean);
 procedure affi_total;

  private
    { private declarations }
  public
    { public declarations }
  end;

var
  f_releve_list: Tf_releve_list;

implementation

{$R *.lfm}

uses u_feuille_style, u_releve_ajout;
end.

